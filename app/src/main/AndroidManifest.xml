<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demotxt.droidsrce.homedashboard">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home" />
        <activity android:name=".ae" />
        >
        <activity android:name=".login" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_fullscreen"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".secondactivity" />
        <activity
            android:name=".thirdactivity"
            android:label="@string/title_activity_thirdactivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".thirdactivityyy" />
        <activity
            android:name=".SpeakUpNow"
            android:label="@string/title_activity_speak_up_now"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ParentGuide" />
        <activity android:name=".KidsEd" />
        <activity android:name=".se2" />
        <activity
            android:name=".Dad"
            android:label="@string/title_activity_dad"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>